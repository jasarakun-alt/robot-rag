# Lahke odvisnosti za zagon vseh storitev + testov (brez torch).
# Realni embeddingi (sentence-transformers) so v services/embedding/requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
numpy>=1.24
pytest>=7.4
